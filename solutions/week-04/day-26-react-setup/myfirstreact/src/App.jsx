// first react component ever, just proving jsx renders. handout wanted App.js with
// a heading, vite calls it App.jsx instead of App.js since it uses the .jsx
// extension by default, same idea though
function App() {
  return (
    <div>
      <h1>welcome to the first session of React</h1>
    </div>
  )
}

export default App
