spring.application.name=account-service
server.port=8081

# this is the name that ends up in the eureka dashboard, and the name loan-service
# or anything else would look this service up by later
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
